import React from 'react';
import { LogStream } from '../components/LogStream';

export const LogsPage: React.FC = () => {
  return (
    <div className="py-6">
      <LogStream />
    </div>
  );
};
